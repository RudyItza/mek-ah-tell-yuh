<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Delete Story | Mek Ah Tell Yuh</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background-image: url('/static/images/folklore-bg.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
        }
        .bg-overlay {
            background-color: rgba(0, 0, 0, 0.6);
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center">
    <div class="bg-overlay w-full h-full absolute z-0"></div>
    <div class="z-10 p-8 rounded-2xl shadow-2xl max-w-md w-full bg-white/90">
        <h2 class="text-3xl font-bold text-center text-gray-800 mb-6">Delete Story</h2>
        <p class="text-gray-700 mb-4">Are you sure you want to delete this story?</p>
        <form method="POST" action="/stories/delete">
            <input type="hidden" name="id" value="{{.ID}}"> <!-- Assuming you pass the story ID -->
            <button type="submit"
                    class="w-full bg-red-600 hover:bg-red-700 text-white font-semibold py-2 rounded-lg transition">
                Delete Story
            </button>
        </form>
        <a href="/stories" class="mt-4 text-center text-sm text-gray-600 hover:underline">Cancel</a>
    </div>
</body>
</html>